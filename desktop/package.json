{
  "name": "starlab-code-desktop",
  "private": true,
  "version": "0.1.8",
  "description": "Windows desktop app for Starlab Code.",
  "main": "src/main.cjs",
  "scripts": {
    "prepare:icons": "node scripts/prepare-icons.cjs",
    "build:frontend": "node scripts/build-frontend.cjs",
    "dev": "electron .",
    "start": "electron .",
    "pack": "npm run prepare:icons && electron-builder --dir",
    "dist": "npm run prepare:icons && npm run build:frontend && electron-builder --win nsis",
    "dist:remote": "npm run prepare:icons && electron-builder --win nsis"
  },
  "dependencies": {
    "koffi": "^2.16.1"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.starlab.code",
    "productName": "Starlab Code",
    "files": [
      "src/**/*",
      "assets/**/*",
      "app/**/*",
      "update-config.json",
      "package.json"
    ],
    "win": {
      "icon": "assets/logo.ico",
      "target": "nsis"
    },
    "nsis": {
      "installerIcon": "assets/logo.ico",
      "uninstallerIcon": "assets/logo.ico",
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
